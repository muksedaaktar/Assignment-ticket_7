## What is JSX, and why is it used?
# Ans: JSX (JavaScript XML) is a syntax extension for JavaScript used in React to allow developers to write code that looks like HTML inside JavaScript.

## What is the difference between State and Props?
# ans: State is  managed data inside a component, props passed data parent to child.

## What is the useState hook, and how does it work?
# Ans: The useState hook is one of the most important hooks in React.It allows a functional component to store, update, and manage data that changes over time.

## How can you share state between components in React?
# Ans: Sharing components in react have several ways:
# 1.Lift State Up
# 2.Using Context API
# 3.Redux / Zustand / Jotai
# 4.URL Params / Query Params

## How is event handling done in React?
# Ans: In React, events are handled using CamelCase props, functions are passed as handlers, and event objects are wrapped as SyntheticEvents for cross-browser consistency.

